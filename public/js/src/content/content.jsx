define(['react'],function (React) {
  
    var Content = React.createClass({
    	render : function () {
    		return (
                <h2>Content here!</h2>
    		);
    	}	
    });

   return Content;
});